CREATE TABLE CategoriesEntity (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    icon TEXT NOT NULL
);

INSERT INTO CategoriesEntity(id, name, icon) VALUES (1, 'Category1', 'ic_category_1');

getAllCategories:
SELECT *
FROM CategoriesEntity;

insertCategory:
INSERT OR REPLACE
INTO CategoriesEntity
VALUES (?,?,?);